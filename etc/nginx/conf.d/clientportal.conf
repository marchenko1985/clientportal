# Upstream backend definitions (resolved once at startup; loopback = no DNS needed).
upstream gateway {
    server 127.0.0.1:5001;  # Gateway or CookieGateway — IBKR REST API proxy
}
upstream feed {
    server 127.0.0.1:5002;  # Feed — WebSocket multiplexer
}
