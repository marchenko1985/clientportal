# Global HTTP-level security settings.
server_tokens    off;   # Hide nginx version from Server: header and error pages
client_max_body_size 1m; # IBKR API payloads are tiny; limit prevents large-body DoS
