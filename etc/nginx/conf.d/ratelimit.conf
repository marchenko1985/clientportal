# Shared rate-limit state for IBKR API requests.
# key=""         INTENTIONAL empty key — all clients share ONE token bucket.
#                IBKR's limit is 10 r/s per account (not per-IP).
#                Using $binary_remote_addr would allow clients to collectively
#                exceed the IBKR limit.
# rate=10r/s     matches IBKR's documented API rate limit
limit_req_zone "" zone=global_api_limit:10m rate=10r/s;
